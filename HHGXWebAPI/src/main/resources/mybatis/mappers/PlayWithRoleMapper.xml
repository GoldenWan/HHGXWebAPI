<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hhgx.soft.mappers.PlayWithRoleMapper">

	<insert id="addManagerSubs" parameterType="managerSubs">

		INSERT INTO managerorg
		(
		ManagerOrgID,
		ManagerOrgName,
		YnSetMonitor,
		tel,
		`Status`,
		ParentID,
		AreaId,
		Remark,
		address,
		ManageOrgGrade,
		PName,
		ManagerJob,
		ContactName,
		ContactTel
		)
		VALUES
		(
		#{managerOrgID}, #{managerOrgName},
		#{ynSetMonitor},
		#{tel},
		#{status}, #{parentID},
		#{areaId}, #{remark},
		#{managerAddress},
		#{manageOrgGrade},
		#{pName}, #{managerJob},
		#{contactName},
		#{contactTel}
		);


	</insert>

	<insert id="addManagerSubsUser" parameterType="user">
		INSERT INTO users
		(
		UserID,
		account,
		PASSWORD,
		UserBelongTo,
		UsertypeID,
		ManagerOrgID
		)
		VALUES
		(#{userID},#{account}, #{password},
		#{userBelongTo},#{userTypeID},#{managerOrgID});

	</insert>

	<delete id="removeManagerSubs">
		DELETE
		FROM
		managerorg
		WHERE
		ManagerOrgID = #{managerOrg};
	</delete>
</mapper>